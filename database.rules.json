{
  "rules": {
    "rooms": {
      "$room": {
        ".read": "$room.length >= 24",
        ".write": "$room.length >= 24",
        ".validate": "newData.hasChildren(['lmp','edd'])"
      }
    }
  }
}
